export enum TRANSACTION_STATUS {
    DONE = 'Done',
    IN_PROGRESS = 'In progress',
    FAILED = 'Failed',
}

export enum REFERRAL_WITHDRAWAL_STATUS {
    APPROVED = 'Approved',
    PENDING = 'Pending',
    FAILED = 'Failed',
}
